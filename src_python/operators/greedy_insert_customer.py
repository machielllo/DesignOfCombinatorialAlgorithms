import numpy as np

def greedy_insert_customer(solution: Solution) -> Solution:
    cids = []
    for request in solution.request_list:
        if request.type == "insert_customer":
            cids.append(customer_id)

    for cid in cids:
        # costs = []
        # for k, v in solution.vehicles.items():
        #     last_idx = v.next_empty_trip()
        #     for idx in range(last_idx):
        #         if v.load[idx] + solution.instance.demands[cid] > solution.instance.volume_capacity:
        #             costs.append(np.inf)
        #         el:
                    
            
    return new_solution



    
