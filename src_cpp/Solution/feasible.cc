#include "solution.ih"

bool Solution::feasible() {
  // check if all customers are served

  // check if battery capacity is never violated

  // check if volume capacity is never violated

  // check if lockers are visited

  return true;
}
